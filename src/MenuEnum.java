import model.RoomType;
/*

public enum MenuEnum {
    ONE("1"),
    TWO("2"),
    THREE("3"),
    FOUR("4"),
    FIVE("5");

    public final String label;
    private MenuEnum(String label) {
        this.label = label;
    }

    public static MenuEnum valueOfLabel(String label) {
        for (MenuEnum menuEnum : values()) {
            if (menuEnum.label.equals(label)) {
                return menuEnum;
            }
        }
        throw new IllegalArgumentException();
    }
}

*/
